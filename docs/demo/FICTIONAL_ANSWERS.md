# 纯虚构面试回答素材

1. 我把耗时操作建模为 BackgroundJob，通过幂等键避免重复创建，通过数据库 claim 和 lease 控制并发，通过 heartbeat 与过期恢复处理 Worker 中断。
2. RAG 结果先检查文件所有权，再形成 Evidence Set；资料不足时降低结论强度，不把普通近似搜索结果直接当作 Agent 已采纳证据。
3. 评价分为技术准确性、资料一致性、回答深度、表达结构和岗位匹配度；分数只用于训练，不声称代表真实招聘结论。
