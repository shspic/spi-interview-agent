# AGENTS.md

用于约束智能体在本项目中的行为，减少常见编码失误。可与项目专属规则合并使用。

取舍说明：本规则更偏向谨慎和可验证，而不是追求最快完成。对于非常简单的任务，可以按常识简化流程。

## 强制语言规则

所有互动、回复、问题描述、计划、总结和代码注释都必须使用简体中文。

例外：
- 代码关键字、命令、文件名、路径、接口名、依赖名可以保留原文。
- 引用已有代码、日志、报错、配置内容时，可以保持原文。

## 1. 编码前先理解

不要假设，不要掩盖不确定性，不要假装已经理解。

在实现前：
- 先读取相关文件和附近代码，理解现有结构、风格和调用关系。
- 明确说明自己的关键假设。
- 如果存在多种合理理解，要说出来，不要默默选择。
- 如果需求不清楚、风险较高或无法从本地上下文判断，先停下来询问。
- 如果有明显更简单的方案，要指出。
- 如果需求本身可能带来问题，可以直接提醒并说明取舍。

## 2. 简单优先

用能解决问题的最小代码。不要做猜测性的扩展。

要求：
- 不实现用户没有要求的功能。
- 不为单次使用抽象。
- 不增加未被要求的灵活性、配置项或兼容层。
- 不为不可能发生的场景编写复杂错误处理。
- 如果实现明显可以更短、更直接，应先简化。
- 不引入新依赖，除非现有工具无法合理完成任务，并且说明原因。

判断标准：每一段新增代码都应该能回答“它直接服务于当前需求吗？”

## 3. 外科手术式修改

只改必须改的地方，只清理自己造成的问题。

修改现有代码时：
- 不顺手重构无关代码。
- 不顺手美化、重排、格式化无关代码。
- 不修改与任务无关的注释、命名或文件结构。
- 匹配项目已有风格，即使你个人会用另一种写法。
- 如果发现无关死代码或可疑问题，只在回复中提及，不主动删除。

处理未提交改动时：
- 把已有未提交改动视为用户工作。
- 不回滚、不覆盖、不格式化用户已有改动，除非用户明确要求。
- 如果用户改动与当前任务冲突，先说明冲突，再询问如何处理。

清理规则：
- 如果你的改动造成了无用导入、变量、函数或文件，应清理。
- 不清理任务开始前就存在的无关问题，除非用户要求。

检验标准：每一行变更都应该能追溯到用户当前请求。

## 4. 删除文件安全规则

禁止批量删除，禁止递归删除。

严禁使用：
- `rm -rf`
- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`

允许的删除方式：
- 一次只删除一个明确指定路径的文件。
- 删除前必须能指出完整文件路径。
- 示例：`Remove-Item "C:\path\to\specific-file.txt"`

如果确实需要批量删除或递归删除：
- 立即停止。
- 说明原因。
- 请用户手动执行，或请用户明确给出下一步指示。

检验标准：如果不能在执行前准确说出要删除的单个文件路径，就不要删除。

## 5. 目标驱动执行

把任务转成可验证目标，然后围绕目标推进。

示例：
- “添加校验”改写为“为非法输入添加测试，然后让测试通过”。
- “修复问题”改写为“先复现问题，再修复，并验证复现用例通过”。
- “重构某模块”改写为“重构前后相关测试都通过，外部行为不变”。

多步骤任务应给出简短计划：
1. 步骤：确认现状；验证：找到相关文件和现有行为。
2. 步骤：做最小修改；验证：检查差异只覆盖必要范围。
3. 步骤：运行相关验证；验证：测试、构建或手动检查通过。

验证要求：
- 优先运行最小相关验证，而不是无差别跑全量流程。
- 能跑测试、构建或静态检查时，应尽量运行。
- 如果无法验证，要明确说明原因。
- 没有实际运行过的验证，不能声称已经通过。

## 6. 命令与工具使用

执行命令前要知道目的，避免为了探索而制造副作用。

规则：
- 搜索文件或文本时，优先使用项目中可用的快速搜索工具。
- 读取、检查、测试优先于修改。
- 不运行会产生大范围改动的命令，除非这是当前任务要求。
- 不安装依赖、不升级依赖、不改锁文件，除非用户明确要求或任务无法完成，并说明原因。
- 不启动长期后台进程，除非任务需要，并在回复中说明如何访问或停止。

## 7. 回复方式

回复要直接、清楚、可执行。

要求：
- 使用简体中文。
- 说明做了什么、验证了什么、还有什么未验证。
- 不夸大结果，不隐藏失败。
- 如果只是发现问题但没有修改，要明确说“未改动文件”。
- 对不确定结论使用“不确定”“需要确认”“根据当前文件判断”等表达。

## 8. 规则有效性的判断

这些规则有效时，应该带来以下结果：

- 差异更小。
- 无关改动更少。
- 过度设计更少。
- 误删和误覆盖更少。
- 智能体会在真正不确定时先问，而不是猜。
- 回复会清楚区分“已验证”和“未验证”。
